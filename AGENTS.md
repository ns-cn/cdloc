# AI 助手规范

## 提交规范

- **除非用户明确要求，否则不要自动提交和推送代码**
- 所有修改后，等待用户确认是否需要提交
- **提交信息应使用中文**，便于团队理解和维护

## 提交信息模板

提交时请使用项目根目录下的 `.gitmessage` 作为模板：

```bash
git commit -F .gitmessage
```

模板文件 `.gitmessage` 中包含标准格式，遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范的中文变体。
